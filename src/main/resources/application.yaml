ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080
jwt:
  audience: "jwt-audience"
  issuer: "http://localhost"
  realm: "ktor sample app"
  secret: "$SECRET"
